// Import the 'is-odd' module
const isOdd = require('is-odd');

// Print to the console whether the number 3 is odd
console.log(isOdd(3));

// Print to the console whether the number 2 is odd
console.log(isOdd(2));

// Import the 'is-even' module
const isEven = require('is-even');

// Print to the console whether the number 3 is even
console.log(isEven(3));

// Print to the console whether the number 2 is even
console.log(isEven(2));